{
  "name": "codex-context-status",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Persistently show live Codex context usage inside the ChatGPT macOS composer.",
  "scripts": {
    "check": "node --check src/injector.mjs && node --check src/native-setting.mjs",
    "test": "TZ=UTC node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
